spring:
  datasource:
    username: iarylser
    password: 1234
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/iarylser
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect